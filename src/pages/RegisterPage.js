import React from 'react';
import {RegisterForm} from "../components";

const RegisterPage = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export default RegisterPage;